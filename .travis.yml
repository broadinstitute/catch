language: python
sudo: false

cache:
 directories:
  - $HOME/virtualenv

python:
  - 3.5.0b3

install:
 - pip install -r requirements.txt
 - pip install coveralls nose-cov
 - python setup.py install --force

script:
 - nosetests -v --with-xunit --with-coverage --cover-inclusive --cover-branches --cover-package hybseldesign

after_success:
 - coveralls
