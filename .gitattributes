# Have git lfs track all data that is part of the repo
# (added by @haydenm on 2017-10-25 during 'git lfs migrate')
hybseldesign/datasets/data/** filter=lfs diff=lfs merge=lfs -text

# Have git lfs track all probe designs saved in the repo
probe-designs/*.fasta.gz filter=lfs diff=lfs merge=lfs -text
probe-designs/*.fasta filter=lfs diff=lfs merge=lfs -text
